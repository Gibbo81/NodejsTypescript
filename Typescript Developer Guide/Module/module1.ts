
//Any declaration (such as a variable, function, class, type alias, or interface) can be exported by adding the export keyword.
export const numberRegexp = /^[0-9]+$/;

export const  obj = {
    x : 10,
    y : 30
} 

const pippo = {name:'pippo'}
export const x = pippo  